animmodel {
	name = "Test"
	num_frames = 1
	
	meshframe {
		frame = 0			// frame number this should show for
		mesh = "..."		// filename
		texture = "..."		// filename
		
		// px, py, pz = position   default 0,0,0 = center
		// rx, ry, rz = rotation   default 0,0,0 = none
		// sz, sy, sz = scale      default 1,1,1 = 1:1
	}
}
